import styled from '@emotion/styled';

import { Typography } from '../../../../components/Typography';

export const PageHeadline = styled(Typography)`
	margin-left: auto;
	margin-right: auto;
	max-width: 100%;
	width: max-content;
`;
